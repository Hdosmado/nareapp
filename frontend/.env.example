# URL base de la API del backend NareApp (incluye el prefijo /api).
VITE_API_URL=http://localhost:3000/api

# Clave de Google Maps JavaScript API para el mapa operativo.
# Si se deja vacía, el mapa muestra un detalle de coordenadas como alternativa.
VITE_GOOGLE_MAPS_API_KEY=
